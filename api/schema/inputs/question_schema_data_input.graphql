# import QuestionSchemaTypeEnum from '../enums/question_schema_type_enum.graphql'

input SchemaDataInput {
  type: QuestionSchemaTypeEnum!
  unique: Boolean
  required: Boolean
  minlength: Int
  maxlength: Int
}